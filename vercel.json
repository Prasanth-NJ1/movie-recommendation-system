{
    "version": 2,
    "builds": [
        { "src": "wsgi.py", "use": "@vercel/python" }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "wsgi.py" }
    ],
    "functions": {
        "wsgi.py": {
            "runtime": "python3.9",
            "includeFiles": ["app.py", "requirements.txt", "backend/**", "frontend/**"]
        }
    }
}
